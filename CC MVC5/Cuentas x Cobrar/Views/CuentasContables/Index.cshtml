@model IEnumerable<CuentasPorCobrar.Domain.CuentasContable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CuentasContables)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CuentasContables)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCuentasContables }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCuentasContables }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCuentasContables })
        </td>
    </tr>
}

</table>
